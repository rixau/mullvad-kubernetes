# Mullvad WireGuard Configuration
# This setup uses config file mounting instead of environment variables

# Instructions:
# 1. Choose your preferred Mullvad server config from conf/ directory
# 2. Update docker-compose.yml to mount your chosen config file:
#    volumes:
#      - ./conf/br-sao-wg-001.conf:/etc/wireguard/wg0.conf:ro
# 3. Run: docker compose up --build

# Available Mullvad servers in conf/ directory:
# - br-sao-wg-001.conf (São Paulo)
# - br-sao-wg-002.conf (São Paulo)
# - br-for-wg-001.conf (Fortaleza)
# - br-for-wg-002.conf (Fortaleza)
# - And more...

# No environment variables needed - config is mounted directly!
